services:
  - type: web
    name: schoolhub
    runtime: static
    buildCommand: bun run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
